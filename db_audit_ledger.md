# Vigile — Audit Base de Données — Ledger Exhaustif

> **Généré le :** 2026-08-31 via SSH youcloud.ovh — inventaire exhaustif avant toute modification.
> **Principe :** chaque ligne correspond à un fichier réel ouvert et vérifié (cat/grep/sqlite3). Statut initial: `à faire` pour tout.
> **Environnement :** toutes les commandes exécutées en SSH sur youcloud.ovh (aucune commande locale). DB réelle inspectée: `data/vigile.db` (SQLite WAL).

| Fichier | Type | Problème identifié | Statut |
|---------|------|--------------------|--------|
| master/db/models.py | modèle / schéma | Définition DDL centralisée (22 tables via CREATE_* + ALL_TABLES). Attend colonnes `nodes.cached_services_at` et `nodes.last_ip` et 3 colonnes `metrics_snapshots.app_sshd_fds_*` qui sont **absentes** de la DB réelle (PRAGMA table_info vide pour ces colonnes) → désynchronisation schéma supposé vs réel. Double création d'index listée à la fois dans CREATE_INDEXES et via migrations.py (idx_nodes_group/disabled). | à faire |
| master/db/migrations.py | migration | Runner idempotent RAW SQL (pas Alembic). Crée ALL_TABLES + CREATE_INDEXES puis patch colonnes via PRAGMA table_info. Contient la migration `cached_services_at` et `last_ip` mais **ne les a pas appliquées** sur data/vigile.db prod (PRAGMA ne les trouve pas) → migration non rejouée ou DB stale. Boucle ``for col,ctype in [(net_*, app_sshd_fds_*, ...)]`` utilise string concat ``ALTER TABLE ... + col + ctype`` mais protégée par \_safe_ddl_identifier (whitelist regex). Stamp alembic_version en ``INSERT OR IGNORE ("009")`` seulement → laisse 4 lignes 006/007/008/009 en prod (SELECT * montre 4 rows). | à faire |
| master/db/database.py | modèle | Pool aiosqlite (Queue maxsize 5, WAL, foreign_keys=ON, busy_timeout 5000). \_pool.acquire() utilise ``asyncio.wait_for`` + health check ``SELECT 1`` + recreate si unhealthy. Risque: \_pool.init() recrée Queue liée à l'event loop courant → si loop change, deadlock. Transaction() utilise ``BEGIN IMMEDIATE`` + reentrance guard ``if db.in_transaction``. Pas d'index ici mais fichier critique à revoir (pool_size fixe 5 non configurable via settings.db_pool_size). | à faire |
| master/db/disk_scan_cache.py | requête directe | 4 fonctions: get_cached_disk_scan / set_cached_disk_scan / get_node_disk_mounts / set_node_disk_mounts. Accès direct `SELECT/UPDATE nodes WHERE id=?` hors couche modèle. set_* utilisent transaction() (OK) mais aucun index dédié sur cached_disks_json. Pas de concaténation (paramétré). | à faire |
| master/db/service_cache.py | requête directe | TTL 300s, contrat {services,count,cached_at,stale,errors}. get_cached_services SELECT + set_cached_services avec garde anti-poison (strip, JSON parse, isinstance list). set utilise transaction(). Index is_stale() basé sur cached_services_at mais cette colonne n'existe pas en prod → is_stale toujours True → cache toujours revalidé. | à faire |
| master/db/alembic/env.py | migration | target_metadata=None (autogenerate désactivé). Lit settings.database_path pour sqlalchemy.url. Offline/online handlers présents mais jamais invoqués en runtime (c'est migrations.py qui tourne). Fichier ouvert = migration morte mais versionnée. | à faire |
| master/db/alembic/script.py.mako | migration | Template de génération Alembic (non utilisé, mais présent). Aucun DDL direct. | à faire |
| alembic.ini | schéma | script_location=master/db/alembic, url sqlite:///data/vigile.db. Désynchronisé: le runner réel est migrations.py raw, pas alembic upgrade → alembic.ini jamais utilisé en prod. | à faire |
| master/db/alembic/versions/001_initial_schema.py | migration | Crée nodes, join_tokens (avec FK), worker_tokens, users, audit_log, metrics_snapshots, action_proposals. Définit idx_nodes_state. FK join_tokens.node_id -> nodes.id sera supprimée en 006 → historique cohérent mais DDL divergé de models.py (models n'a plus de FK sur join_tokens). | à faire |
| master/db/alembic/versions/002_add_refresh_tokens.py | migration | Crée refresh_tokens + 2 index (user, family). Ok, mais upgrade non rejoué via alembic (versionée mais exécutée via raw). | à faire |
| master/db/alembic/versions/003_add_must_change_password.py | migration | batch_alter_table users ADD COLUMN must_change_password server_default 0. Idempotent via migrations.py mais alembic path inutilisé. | à faire |
| master/db/alembic/versions/004_add_insights_columns.py | migration | Ajoute nodes.insight_profile, insight_profile_generated_at, cached_services_json, cached_containers_json. **Omet cached_services_at** → cause directe du missing column prod. | à faire |
| master/db/alembic/versions/005_add_group_and_disabled.py | migration | Ajoute node_group, disabled + index idx_nodes_group/disabled. Ok mais migrations.py duplique ces index en fin de run (double CREATE IF NOT EXISTS). | à faire |
| master/db/alembic/versions/006_drop_join_tokens_fk.py | migration | batch_alter drop FK join_tokens.node_id → implémentée réellement dans migrations.py via table rebuild (join_tokens_new) + PRAGMA foreign_keys OFF/ON. Alembic version est no-op batch drop non testé (SQLite batch ne supporte pas drop FK sans rebuild). | à faire |
| master/db/alembic/versions/007_associate_rules_with_alert_names.py | migration | upgrade() = pass / downgrade() = pass → migration vide historisée (006→007). Aucune DDL mais version bump. Cause 007 d'alembic_version en prod sans effet. | à faire |
| master/db/alembic/versions/008_plugin_configs_to_plugins.py | migration | upgrade() = pass (stub). La vraie migration est dans migrations.py _migrate_plugin_configs_to_plugins (INSERT OR IGNORE SELECT + DROP plugin_configs). Désynchronisation Alembic vs raw runner. | à faire |
| master/db/alembic/versions/009_add_disk_scan_columns.py | migration | Ajoute cached_disk_scan_json + cached_disk_scan_at. Ok mais ne couvre pas last_ip ni cached_services_at ni cached_disks_json réels. | à faire |
| master/core/audit.py | requête directe | get_recent_audit_logs: construit ``where = ("WHERE " + " AND ".join(conditions))`` puis ``f"SELECT ... FROM audit_log {where} ORDER BY sequence DESC LIMIT ?"`` avec params ? (paramétré). Pas de concat de valeur. Log d'audit hash chain verify_chain lit toute la table (pas d'index sur timestamp). Appels hors modèle (route /api/audit). | à faire |
| master/core/node_manager.py | requête directe | Fichier le plus critique: 15+ execute. Dynamic ``query = "UPDATE nodes SET " + ", ".join(fields) + " WHERE id = ?"`` (ligne 310) et ``set_clause = ", ".join(f"{k} = ?" for k in fields)`` (538,764) — safe car k vient de _VALID_NODE_FIELDS whitelist, valeurs via ?. Contient _VALID_NODE_FIELDS sans last_ip initialement (patché en Session E2). Méthodes is_connected, transition_state, heartbeat utilisent SELECT state/disabled sans index sur disabled+state composite. | à faire |
| master/core/alert_engine.py | requête directe | DELETE FROM alerts WHERE node_id NOT IN (SELECT id FROM nodes) pour orphan cleanup. SELECT alerts par node_id/status avec index existants (alerts_node_status ok) mais requêtes GROUP BY severity sans index couvrant. | à faire |
| master/core/insights.py | requête directe | SELECT * FROM metrics_snapshots WHERE node_id=? ORDER BY collected_at DESC LIMIT 1 (x2). Boucles _calculate_disk_insight et _compute_synthetic_metrics lisent metrics_snapshots sans index sur top_processes_json/disks_json (colonnes JSON). Pas de concat. | à faire |
| master/core/investigation_manager.py | requête directe | ``where = (" WHERE " + " AND ".join(conditions))`` puis ``f"SELECT * FROM investigations{where} ORDER BY created_at DESC LIMIT ? OFFSET ?"`` paramétré. Filtres node_id/status ont indexes (investigations_node/status) OK. Accès direct hors modèle. | à faire |
| master/core/outbox.py | requête directe | SELECT * FROM outbox WHERE processed=0 ORDER BY created_at ASC (claim outbox). Index idx_outbox_unprocessed(processed, created_at) présent. UPDATE processed=1/2 avec retry_count. Direct. | à faire |
| master/core/security_manager.py | requête directe | SELECT revoked FROM worker_tokens WHERE token_hash=? (index worker_tokens_hash). SELECT * FROM join_tokens / worker_tokens pour enrollment. Pas de concat. | à faire |
| master/core/proposal_dispatcher.py | requête directe | SELECT * FROM action_proposals WHERE id=? + SELECT dispatch_id,status. UPDATE action_proposals status/dispatch_id. Pas d'index sur dispatch_id (colonne récente, non indexée alors que filtrée en "WHERE dispatch_id IS NULL" lifespan). | à faire |
| master/core/proposal_autoexpire.py | requête directe | **Concat f-string à risque maîtrisé**: ``f"SELECT {metric}, collected_at FROM metrics_snapshots ..."`` où metric whitelisté via _RESOURCE_KEYWORDS (disk_percent/mem_percent/cpu_percent/load) puis break. Commentaire SAFE explicit. Reste à auditer whitelist exhaustive. SELECT * FROM action_proposals WHERE status='PENDING' (index proposals_status). | à faire |
| master/core/plugin_engine.py | requête directe | SELECT id,enabled FROM plugins; SELECT config_json FROM plugins WHERE id=?; UPDATE plugins SET status='ERROR'/'ACTIVE'/'DISABLED'; DELETE FROM plugins WHERE id=? — accès direct sur table plugins hors modèle. Index plugins_enabled/status existent. | à faire |
| master/core/plugin_manager.py | requête directe | SELECT id FROM plugins WHERE enabled=0 + SELECT config_json WHERE id=? . Même table, même remarque. | à faire |
| master/core/plugin_lifecycle.py | requête directe | SELECT enabled FROM plugins WHERE id=?; SELECT id FROM plugins WHERE status IN ('LOADING','STOPPING') — sans index composite sur status (mais status indexé seul). | à faire |
| master/core/policy_engine.py | requête directe | SELECT * FROM policies WHERE node_id=? ORDER BY policy_epoch DESC, policy_version DESC — index policies_node_ver présent (node_id, epoch, version DESC). | à faire |
| master/core/db_auto.py | requête directe | Fichier de migration auto plugin (db helpers) — exécute DDL pour plugin sandbox. Utilise _safe_ddl_identifier mais hors couche modèle. | à faire |
| master/core/plugin_base.py | modèle / requête directe | Définit PluginBase.ctx.db_query() avec garde: n'autorise que SELECT et vérifie préfixe table ("plex_", "node_", etc.) + bloque FROM SQLITE_MASTER. Problème: prefix check est ``table.startswith(prefix)`` mais allowed prefixes list inclut "nodes", "plugins" → OK, mais implémente deny sinon. Utilisé par plugins. | à faire |
| master/core/jobs/service_collector.py | requête directe | collect_services_for_all_nodes: SELECT nodes list, Semaphore(5) + port.query. Ecrit via set_cached_services only-if-success guard. Accès direct. | à faire |
| master/api/nodes.py | requête directe | 8 execute: SELECT id,cached_containers_json FROM nodes (scan global sans WHERE — full table scan, pas indexé), SELECT * FROM nodes WHERE id=?, SELECT disks_json FROM metrics_snapshots etc. Mirror avec nodes_management.py (duplication DDL). Contient "SELECT COUNT(*) FROM audit_log " + where (audit compat). | à faire |
| master/api/nodes_management.py | requête directe | Miroir dead-code de nodes.py (AGENTS.md: synchronisation requise). Même requêtes dupliquées — toute modif doit être dupliquée, dette. | à faire |
| master/api/nodes_helpers.py | requête directe | _node_to_response mappe node.get("last_ip") (attendu mais prod NULL). Requêtes helpers SELECT * FROM nodes. | à faire |
| master/api/audit.py | requête directe | ``where = ("WHERE " + " AND ".join(conditions))`` + ``count_sql = "SELECT COUNT(*) as cnt FROM audit_log " + where`` + ``sql = "SELECT ... FROM audit_log " + where + " ORDER BY sequence DESC LIMIT ? OFFSET ?"`` — paramétré via params, pas de concat valeur. Méthode paginée. | à faire |
| master/api/auth.py | requête directe | 12 execute: SELECT FROM users WHERE username=? / id=?, SELECT FROM refresh_tokens WHERE token_hash=?, UPDATE refresh_tokens SET revoked... WHERE user_id=?, etc. users.username est UNIQUE index implicite, mais WHERE role=? n'a pas d'index (peu filtré). Direct hors modèle. | à faire |
| master/api/metrics.py | requête directe | 7 execute: SELECT COUNT(*) FROM action_proposals WHERE status=?, SELECT state COUNT GROUP BY, SELECT COUNT WHERE state='LOST', SELECT severity alert_name GROUP BY, SELECT total/connected FROM nodes. Agrégations sans index couvrant sur state/status. | à faire |
| master/api/admin.py | requête directe | 17 execute: SELECT FROM plugins, SELECT COUNT(*) FROM alerts WHERE + where (concat ``count_sql = "SELECT ... WHERE " + where`` avec where construit depuis params ?), SELECT GROUP BY severity/status, etc. Direct hors modèle le plus dense. | à faire |
| master/api/chat_sessions.py | requête directe | 9 execute: SELECT * FROM chat_sessions WHERE user_id=? [AND node_id=?] ORDER BY is_pinned DESC, updated_at DESC — index chat_sessions_user/node séparés mais pas composite (user_id, node_id) ni (is_pinned, updated_at) → tri filesort. | à faire |
| master/api/chat_proposals.py | requête directe | 9 execute: SELECT * FROM action_proposals WHERE status=? ORDER BY created_at DESC, WHERE id=? — index proposals_status/node existent. Direct. | à faire |
| master/api/chat_helpers.py | requête directe | 4 execute: SELECT cpu/mem/disk FROM metrics_snapshots WHERE node_id=? ORDER BY collected_at DESC LIMIT 1; SELECT cached_containers_json FROM nodes WHERE id=? — index node_time. | à faire |
| master/api/chat_stream.py | requête directe | 4 execute: SELECT history_json/title FROM chat_sessions WHERE id=? AND user_id=?; UPDATE chat_sessions SET history_json... — même remarque index composite manquant. | à faire |
| master/api/deps.py | requête directe | 1 execute: SELECT is_active, must_change_password FROM users WHERE id=? — index users PK. | à faire |
| master/api/demo.py | requête directe | DELETE FROM action_proposals sans WHERE (truncate demo). Hors modèle. | à faire |
| master/ws/worker_handler.py | requête directe | 15+ execute: SELECT state,public_key FROM nodes WHERE id=?, transaction sur enrollment (INSERT INTO nodes avec last_ip, UPDATE last_ip=COALESCE), SELECT disabled, INSERT join_tokens, etc. Logique métier majeure hors modèle. | à faire |
| master/lifespan.py | requête directe | SELECT id,node_id FROM action_proposals WHERE status='APPROVED' AND dispatch_id IS NULL — sans index sur dispatch_id (NULL scan). SELECT last_heartbeat FROM nodes WHERE id=? . Gestion outbox replay. | à faire |
| master/auto_update.py | requête directe | SELECT id,name,version,state FROM nodes WHERE state != 'REVOKED' — filtre state sans index composite state+version. | à faire |
| master/plugins/metrics/__init__.py | requête directe | INSERT INTO metrics_snapshots (...) via _on_status_report (colonnes net/disk/temp/psi/file_handles mais **omet app_sshd_fds_used/max/percent** si DB manque → INSERT échoue silencieusement loggé). SELECT node_id,collected_at... FROM metrics_snapshots WHERE collected_at>=? AND node_id=? ORDER BY collected_at ASC (history). | à faire |
| master/plugins/docker/__init__.py | requête directe | Import get_db, route list_containers avec db:Depends(get_db) mais n'utilise pas db (WorkerQueryPort only). Fichier ouvert mais requêtes annulées. | à faire |
| master/plugins/systemd/__init__.py | requête directe | list_services_route utilise service_cache helpers (SELECT cached_services_json...) + fallback port.query 10s/15s. Accès direct. | à faire |
| master/plugins/plex/__init__.py | requête directe | 20+ execute: SELECT config_json FROM plugins WHERE id='plex', INSERT/DELETE plex_watch_history WHERE viewed_at<? , SELECT COUNT(*) + SELECT ... FROM plex_watch_history WHERE ``where_str = " AND ".join(where_clauses)`` puis ``f"SELECT COUNT(*) FROM plex_watch_history WHERE {where_str}"`` et ``f"SELECT ... WHERE {where_str} ORDER BY viewed_at DESC LIMIT ? OFFSET ?"`` — where_clauses whitelistés (media_type=?, user LIKE ?, title LIKE ?) paramétrés, pas de concat valeur brute, mais f-string injecte where_str (qui est safe car non-user-string). Accès direct massif hors modèle. Import aiosqlite + get_db_conn. | à faire |
| master/plugins/clean_logs/__init__.py | requête directe | cursor = await db.execute("SELECT ... FROM ...") + await db.execute("DELETE ...") — routes génériques via db_query prefix. | à faire |
| data/vigile.db (réel) | schéma | **Désynchronisation critique**: PRAGMA table_info(nodes) = 24 cols, manque `cached_services_at REAL` et `last_ip TEXT` attendus par models.py:cached_services_at/last_ip (SESSION E2). `sqlite_master nodes` montre string tronquée sur cached_disks_json. `alembic_version` contient 4 rows (006,007,008,009) au lieu d'une seule 009. Tables orphelines non listées dans models.py: `automation_rules`, `automation_cooldowns`, `automation_logs`, `metrics_metrics_snapshots` (double). Index sqlite_autoindexes orphelins correspondants. metrics_snapshots manque 3 colonnes app_sshd_fds_* (PRAGMA ne les liste pas) alors que models.py et migrations.py les attendent. | à faire |
| master/db/models.py | schéma | CREATE_INDEXES liste 33 index mais **manques**: pas d'index sur `action_proposals.dispatch_id` (filtré lifespan), pas d'index sur `action_proposals.expires_at`, pas d'index composite `chat_sessions(user_id, node_id)`, pas d'index sur `metrics_snapshots(collected_at)` seul, pas d'index sur `nodes(last_heartbeat)` (reaper), pas d'index sur `plugins.manifest_hash`. Inversement, index existants non listés dans models: idx_nodes_group/disabled ajoutés seulement dans migrations.py. | à faire |
| master/core/node_manager.py | requête directe | get_nodes(filters) construit ``WHERE " + " AND ".join(clauses)`` avec clauses sur state/node_group/disabled — state a index, node_group/disabled ont index ajoutés en migrations.py mais pas dans CREATE_INDEXES initial → risque de table scan si DB fraîche sans migrations. | à faire |
| master/api/audit.py | requête directe | Pattern répété ``"WHERE " + " AND ".join(conditions)`` + f-string SQL — **pas d'injection** (conditions = "col = ?") mais 4 fichiers dupliquent ce pattern sans helper centralisé → dette. | à faire |
| scripts/gen_token.py | requête directe | Scripts hors API avec ``import sqlite3`` + ``sqlite3.connect('/app/data/vigile.db')`` (gen_token) et ``aiosqlite.connect`` (autres). Accès direct hors couche modèle, non traçé par audit log. | à faire |
| tests/conftest.py | requête directe | 30+ fichiers tests utilisent ``await db.execute("INSERT/SELECT/DELETE FROM ...")`` pour fixtures / assertions — légitime mais masque désynchro prod (tests passent sur DB mémoire fraîche via run_migrations, pas sur data/vigile.db prod stale). | à faire |
| master/plugins/plex/__init__.py | requête directe | Lignes 1291-1292: ``count_sql = f"SELECT COUNT(*) FROM plex_watch_history WHERE {where_str}"`` et ``select_sql = f"... WHERE {where_str} ORDER BY ..."`` — where_str vient de ``" AND ".join(where_clauses)`` où where_clauses sont hardcodés, donc safe mais **techniquement concaténation SQL** à documenter (critère 3). Même pattern dans proposal_autoexpire.py:129. | à faire |

**Total lignes trouvées:** 60
