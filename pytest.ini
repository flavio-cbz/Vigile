[pytest]
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
addopts = --ignore=tests/unit --ignore=tests/integration
markers =
    integration: tests requiring a running server
